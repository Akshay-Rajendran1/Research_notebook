# What I have done 
I added a parser match for delete, I added another function to handle the del command. I changed the command enum to handle this command too, finaly I added a match part to deal with the del inside the client.rs to handle the code.

# Issues 
None this command was similar to the add command I had to add before for add.

# Goals
Patch out any issues and there might be and make sure the ips that are added actually are used.
